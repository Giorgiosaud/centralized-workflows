name: test wf

on:
  push:
    branches:
      - test

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Push changes
        run: |
          echo $PRUEBA
          echo https://github.com/orgs/${GITHUB_REPOSITORY_OWNER}/packages?repo_name=${GITHUB_REPOSITORY/$GITHUB_REPOSITORY_OWNER\//}
        env:
          PRUEBA: HOLA